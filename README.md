# 實體行前信審閱台（內部審稿用）

橘子蘋果實體試聽行前通知信十六封的逐封審閱頁，給同事在瀏覽器裡檢查用。

- 產生器：`~/Documents/OA_行前通知信改版/generator_physical/`（改信後重跑 `build.py` 再跑 `build_review.py`）
- 本 repo 只放產生結果，請勿直接編輯 `index.html`，下次產生會被覆蓋。
- 已加 `noindex` 與 `robots.txt`，不希望被搜尋引擎收錄。
- **審稿結束後請關閉 GitHub Pages 並刪除本 repo。**
